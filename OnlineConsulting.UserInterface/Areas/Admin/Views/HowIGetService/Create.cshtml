@model CreateHowIGetServiceDto
@{
    ViewData["Title"] = "Create New How I Get Service Stage";
    ViewData["SubPage1"] = "How I Get Service Stage List";
    ViewData["SubPage1Url"] = Url.Action("Index", "HowIGetService", new { area = "Admin" });
    ViewData["SubPage2"] = "Create How I Get Service Stage";
    ViewData["SubPage2Url"] = Url.Action("Create", "HowIGetService", new { area = "Admin" });
}
<div class="col-12">
    <div class="card p-3">
        <div class="card-header">
            <h5 class="mb-0">Create New How I Get Service Stage</h5>
        </div>
        <div class="card-body">
            <form method="post" enctype="multipart/form-data">
                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="imgicon">Icon</label>
                            <select class="form-control" id="imgicon" asp-for="ImgIconId">
                                <option value="" disabled selected>Select an icon</option>
                                @foreach (var icon in (List<ResultImgIconDto>)ViewBag.GetImgIcons)
                                {
                                    <option value="@icon.Id">@icon.Name</option>
                                }
                            </select>
                            <span asp-validation-for="ImgIconId" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label" for="title">Title</label>
                            <input type="text" class="form-control" id="title" placeholder="Enter title" asp-for="Title" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="col-md-12">
                        <div class="mb-3">
                            <label class="form-label" for="descriptionTextarea">Descripton</label>
                            <textarea class="form-control" id="descriptionTextarea" rows="3" placeholder="Enter description" asp-for="Description"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="col-md-12 text-end mt-3">
                        <div class="d-grid gap-2 mt-2 text-center">
                            <button class="btn btn-outline-primary d-inline-flex text-center justify-content-center"> <i class="ti ti-bookmark me-1"></i>Save</button>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>