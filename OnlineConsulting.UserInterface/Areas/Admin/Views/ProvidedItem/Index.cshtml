@using OnlineConsulting.UserInterface.Areas.Admin.Features.ProvidedItem
@model List<ProvidedItemListItemViewModel>
@{
    ViewData["Title"] = "Provided Item";
    ViewData["SubPage1"] = "Provided Item";
    ViewData["SubPage1Url"] = Url.Action("Index", "ProvidedItem", new { area = "Admin" });
    int count = 1;
}

<script type="module">
    import { DataTable } from '/templates/admin-sky-template/able-pro-vanila-bootstrap-9.5.0/dist/assets/js/plugins/module.js';
    window.dt = new DataTable('#pc-dt-simple');
</script>

<div class="col-12">
    <div class="card table-card">
        <div class="card-header">
            <div class="d-sm-flex align-items-center justify-content-between">
                <h5 class="mb-3 mb-sm-0">Provided Item List</h5>
                <div>
                    <a href="@Url.Action("Create","ProvidedItem")" class="btn btn-primary">Add Provided Item</a>
                </div>
            </div>
        </div>
        <div class="card-body pt-3">
            @if (Model is { Count: > 0 })
            {
                <div class="table-responsive">
                    <table class="table table-hover" id="pc-dt-simple" style="table-layout: auto; width: 100%;">
                        <thead>
                            <tr>
                                <th style="padding-left:2rem">Count</th>
                                <th style="word-wrap: break-word; white-space: normal;">Icon - Title</th>
                                <th style="word-wrap: break-word; white-space: normal;">Description</th>
                                <th style="word-wrap: break-word; white-space: normal;">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td style="word-wrap: break-word; white-space: normal;">@count</td>
                                    <td style="word-wrap: break-word; white-space: normal;">
                                        <div class="d-flex align-items-center justify-content-center">
                                            <div class="flex-shrink-0">
                                                <i class="@item.Icon f-24" style="color: @(item.IconColor ?? "inherit")"></i>
                                            </div>
                                            <div class="flex-grow-1 ms-3">
                                                <h6 class="mb-0">@item.Title</h6>
                                            </div>
                                        </div>
                                    </td>
                                    <td style="word-wrap: break-word; white-space: normal;">@item.Description</td>
                                    <td>
                                        <a href="#" data-bs-toggle="modal" data-bs-target="#modal-@item.Id" class="avtar avtar-xs btn-link-secondary detail-btn">
                                            <i class="ti ti-eye f-20"></i>
                                        </a>
                                        <a href="@Url.Action("Update","ProvidedItem",new{id=item.Id})" class="avtar avtar-xs btn-link-secondary">
                                            <i class="ti ti-edit f-20"></i>
                                        </a>
                                        <a href="#" data-bs-toggle="modal" data-bs-target="#deleteModalCenter" class="avtar avtar-xs btn-link-secondary delete-btn" data-id="@item.Id">
                                            <i class="ti ti-trash f-20"></i>
                                        </a>
                                    </td>
                                </tr>
                                count++;
                            }
                        </tbody>
                    </table>
                </div>
                <!-- Detail Modal Start -->
                @foreach (var item in Model)
                {
                    <div id="modal-@item.Id" class="modal fade" tabindex="-1" aria-labelledby="detailModalCenterTitle" aria-hidden="true">
                        <div class="modal-dialog modal-dialog-centered">
                            <div class="modal-content shadow-lg rounded-3 border-0">
                                <div class="modal-header bg-light border-0">
                                    <h5 class="modal-title fw-bold text-dark">@item.Title</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                </div>
                                <div class="modal-body p-4">
                                    <div class="row align-items-center">
                                        <div class="col-md-4 text-center">
                                            <i class="@item.Icon" style="font-size: 64px; color: @(item.IconColor ?? "inherit")"></i>
                                        </div>
                                        <div class="col-md-8">
                                            <h6 class="text-muted">@item.Title</h6>
                                            <p class="text-secondary">@item.Description</p>
                                        </div>
                                    </div>
                                </div>
                                <div class="modal-footer border-0">
                                    <button type="button" class="btn btn-outline-secondary px-4" data-bs-dismiss="modal">Close</button>
                                </div>
                            </div>
                        </div>
                    </div>
                }
                <!-- Detail Modal End -->
            }
            else
            {
                <div class="alert alert-success m-4" role="alert"> No data added yet! </div>
            }
        </div>
    </div>
</div>
<!-- Delete Confirmation Modal Start -->
<div id="deleteModalCenter" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="deleteModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalCenterTitle">Delete Confirmation</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>
                    The record you selected will be deleted. Do you want to continue?
                </p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-success" data-bs-dismiss="modal">No</button>
                <a href="#" id="confirmDeleteBtn" class="btn btn-danger">Yes</a>
            </div>
        </div>
    </div>
</div>
<!-- Delete Confirmation Modal End -->
<script>
        document.addEventListener('DOMContentLoaded', function () {
        const deleteButtons = document.querySelectorAll('.delete-btn');
        const confirmDeleteBtn = document.getElementById('confirmDeleteBtn');

        deleteButtons.forEach(button => {
            button.addEventListener('click', function () {
                const itemId = this.getAttribute('data-id');
                confirmDeleteBtn.setAttribute('href', `/admin/provided-items/${itemId}/delete`);
            });
        });
    });
</script>
