@using OnlineConsulting.UserInterface.Areas.Admin.Features.Service
@model UpdateServiceViewModel
@{
    ViewData["Title"] = "Update Service";
    ViewData["SubPage1"] = "Service";
    ViewData["SubPage1Url"] = Url.Action("Index", "Service", new { area = "Admin" });
    ViewData["SubPage2"] = "Update Service";
    ViewData["SubPage2Url"] = Url.Action("Update", "Service", new { area = "Admin", id = Model.Id });
}

<div class="col-12">
    <div class="card p-3">
        <div class="card-header">
            <h5 class="mb-0">@Model.Title</h5>
        </div>
        <div class="card-body">
            <div class="col">
                <div class="row">
                    <div class="col-md-12">
                        <div class="row">
                            @if (Model.ExistingImages.Any())
                            {
                                foreach (var image in Model.ExistingImages)
                                {
                                    <div class="col-sm-6 col-lg-4 col-xxl-3">
                                        <div class="position-relative">
                                            <img src="@image.Url" alt="img" class="img-fluid w-100 rounded-3" />
                                            @if (image.IsCover)
                                            {
                                                <span class="badge bg-primary" style="position: absolute; top: 10px; left: 10px;">Cover</span>
                                            }
                                            <div style="position: absolute; bottom: 10px; right: 10px;">
                                                <a href="#" data-bs-toggle="modal" data-bs-target="#deleteModalCenter" class="btn btn-sm btn-outline-danger delete-btn" data-id="@image.Id" data-modelId="@Model.Id">
                                                    <i class="ti ti-trash f-20"></i>
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                }
                            }
                            else
                            {
                                <div class="col-sm-12 col-lg-12 col-xxl-12">
                                    <div class="alert alert-success col-12" role="alert"> No image added yet! </div>
                                </div>
                            }
                        </div>
                    </div>
                    <div class="col-md-12">
                        <div class="mt-3">
                            <form action="/admin/services/@Model.Id/media-items" class="row g-1 align-items-center" method="post" enctype="multipart/form-data">
                                <!-- Input Field -->
                                <div class="col-md-12">
                                    <input name="image" class="form-control" id="ServiceImageimage" type="file" accept=".png, .jpeg, .jpg, .gif" required />
                                    <div class="text-muted" style="font-size: x-small;">
                                        <span style="font-weight: bold;">*</span>Please ensure the image is <strong>370x240 pixels</strong> for the best results.
                                    </div>
                                </div>
                                <!-- Button -->
                                <div class="col-md-12 text-end mt-3">
                                    <div class="d-grid gap-2 mt-2 text-center">
                                        <button class="btn btn-outline-primary d-inline-flex text-center justify-content-center"> <i class="ti ti-bookmark me-1"></i>Save</button>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
                <hr />
                <div class="col">
                    <form method="post">
                        <div class="row">
                            <input type="hidden" asp-for="Id" />
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label" for="Servicecategory">Category</label>
                                    <select class="form-control" id="Servicecategory" asp-for="CategoryId">
                                        <option value="" disabled selected>Select a category</option>
                                        @foreach (var category in Model.Categories)
                                        {
                                            <option value="@category.Id">@category.Title</option>
                                        }
                                    </select>
                                    <span asp-validation-for="CategoryId" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label" for="title">Title</label>
                                    <input type="text" class="form-control" id="title" placeholder="Enter title" asp-for="Title" />
                                    <span asp-validation-for="Title" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label" for="price">Price</label>
                                    <input type="number" class="form-control" id="price" placeholder="Enter price" asp-for="Price" />
                                    <span asp-validation-for="Price" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label" for="discountrate">Discount Rate (%)</label>
                                    <input type="number" class="form-control" id="discountrate" placeholder="Enter discount rate" asp-for="DiscountRate" />
                                    <span asp-validation-for="DiscountRate" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label" for="discountedprice">Discounted Price</label>
                                    <input type="number" class="form-control" id="discountedprice" placeholder="Calculated from price and discount rate" disabled />
                                    <input type="hidden" id="discountedprice-hidden" />
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label" for="taxrate">Tax Rate (%)</label>
                                    <input type="number" class="form-control" id="taxrate" placeholder="Enter tax rate" asp-for="TaxRate" />
                                    <span asp-validation-for="TaxRate" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label" for="featuredarea">Featured Area</label>
                                    <select class="form-control" id="featuredarea" asp-for="FeaturedArea">
                                        <option value="" disabled selected>Select an option</option>
                                        <option value="True">Yes</option>
                                        <option value="False">No</option>
                                    </select>
                                    <span asp-validation-for="FeaturedArea" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-12">
                                <div class="mb-3">
                                    <label class="form-label" for="descripton">Descripton</label>
                                    <textarea class="form-control" id="descripton" rows="3" placeholder="Enter description" asp-for="Description"></textarea>
                                    <span asp-validation-for="Description" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-12">
                                <div class="mb-3">
                                    <label class="form-label" for="summernote-textarea">Detailed Descripton</label>
                                    <textarea class="form-control" id="summernote-textarea" rows="3" placeholder="Enter detailed description" asp-for="DetailedDescription"></textarea>
                                    <span asp-validation-for="DetailedDescription" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-12 text-end mt-3">
                                <div class="d-grid gap-2 mt-2 text-center">
                                    <button class="btn btn-outline-primary d-inline-flex text-center justify-content-center"> <i class="ti ti-bookmark me-1"></i>Update</button>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
<div id="deleteModalCenter" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="deleteModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalCenterTitle">Delete Confirmation</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>
                    The record you selected will be deleted. Do you want to continue?
                </p>
            </div>
            <div class="modal-footer">
                <form id="deleteImageForm" method="post">
                    <button type="button" class="btn btn-success" data-bs-dismiss="modal">No</button>
                    <button id="confirmDeleteBtn" class="btn btn-danger">Yes</button>
                </form>

            </div>
        </div>
    </div>
</div>
<script>
        document.addEventListener('DOMContentLoaded', function () {
        const deleteButtons = document.querySelectorAll('.delete-btn');
        const deleteImageForm = document.getElementById('deleteImageForm');

        deleteButtons.forEach(button => {
            button.addEventListener('click', function () {
                const itemId = this.getAttribute('data-id');
                const modelId = this.getAttribute('data-modelId');
                deleteImageForm.setAttribute('action', `/admin/services/${modelId}/media-items/${itemId}/delete`);
            });
        });
    });
      document.addEventListener("DOMContentLoaded", function () {
        const priceInput = document.getElementById("price");
        const discountRateInput = document.getElementById("discountrate");
        const discountedPriceInput = document.getElementById("discountedprice");
        const discountedPriceHiddenInput = document.getElementById("discountedprice-hidden");

        function calculateDiscountedPrice() {
            let price = parseFloat(priceInput.value) || 0;
            let discountRate = parseFloat(discountRateInput.value) || 0;

            if (price > 0 && discountRate >= 0 && discountRate <= 100) {
                let discountedPrice = price - (price * discountRate / 100);
                discountedPriceInput.value = discountedPrice.toFixed(2);
                discountedPriceHiddenInput.value = discountedPrice.toFixed(2);
            } else {
                discountedPriceInput.value = "";
                discountedPriceHiddenInput.value = "";
            }
        }

        priceInput.addEventListener("input", calculateDiscountedPrice);
        discountRateInput.addEventListener("input", calculateDiscountedPrice);
    });
</script>