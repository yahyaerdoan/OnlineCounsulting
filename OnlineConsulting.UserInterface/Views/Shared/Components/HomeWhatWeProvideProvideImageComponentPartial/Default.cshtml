@model IQueryable<ResultWhatWeProvideDto>

<div class="col-lg-6">
    <div class="provide-image text-end wow fadeInBottom" data-wow-duration="0.6s">
        @foreach (var item in Model)
        {
            <img src="@item.ImageUrl" alt="" style="border-radius:0.6rem">
        }
    </div>
</div>